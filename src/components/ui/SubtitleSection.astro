---
interface Props {
  title: string;
  className?: string;
}

const { title } = Astro.props;
---

<div class="flex items-center gap-3 mb-8">
  <span class="h-px flex-1 bg-line2"></span>
  <p class="font-serif text-xs tracking-[0.35em] text-gold uppercase">
    {title}
  </p>
  <span class="h-px flex-1 bg-line2"></span>
</div>