---
import { twMerge } from 'tailwind-merge'

interface Props {
  title: string
  className?: string
  id?: string
}
const { title, className, id } = Astro.props
---

<div class={twMerge("flex items-center gap-4 mb-16", className)}>
  <span class="h-px flex-1 bg-line2"></span>
  <h2 class="font-bold text-2xl md:text-3xl tracking-tight text-ink" id={id}>{title}</h2>
  <span class="h-px flex-1 bg-line2"></span>
</div>