---
import CtaButton from "./CtaButton.astro"

const navItems = [
  { name: 'Inicio', href: '/#home', label: 'home' },
  { name: 'Servicios', href: '/#projects', label: 'projects' },
  { name: 'Nosotros', href: '/#about', label: 'about' }
]
---

<header class="fixed top-0 inset-x-0 z-50 backdrop-blur-lg">
  <nav class="max-w-4xl mx-auto px-6 h-14 flex items-center justify-center sm:justify-between">
    <a href="/#home" class="hidden sm:inline-flex items-center gap-3 font-mono text-sm font-medium tracking-widest text-ink">
      <img src="/logo.png" alt="Triblan logo" class="h-8 w-auto rounded-full border border-line2 bg-surface" />
      TRIBLAN
    </a>
    <ul class="flex sm:gap-8 gap-4 list-none items-center">
      {
        navItems.map(item => (
          <li class="cursor-pointer hover:text-lime transition-colors text-nowrap">
            <CtaButton
              ariaLabel={item.label}
              variant="text"
              href={item.href}
            >
              {item.name}
            </CtaButton>
          </li>
        ))
      }
      <li>
        <CtaButton
          ariaLabel='contact'
          variant="outline-sm"
          href="/#contact"
        >
          Contacto
        </CtaButton>
      </li>
    </ul>
  </nav>
</header>

<script>
  document.addEventListener("astro:page-load", () => {
    const sections = document.querySelectorAll<HTMLElement>('section[id]')
    const navLinks = document.querySelectorAll<HTMLAnchorElement>('nav ul li a')
    
    const callback = (entries: IntersectionObserverEntry[]) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          navLinks.forEach(link => {
            link.classList.remove('text-lime')
            if (link.getAttribute('aria-label') === entry.target.id) {
              link.classList.add('text-lime')
            }
          })
        }
      })
    }

    const observer = new IntersectionObserver(callback, {
      root: null,
      rootMargin: '-80px 0px -50% 0px',
      // threshold: 0.5
    })

    sections.forEach(section => observer.observe(section))

    document.onvisibilitychange = () => {
      if (document.visibilityState === 'hidden') {
        observer.disconnect()
      } else {
        sections.forEach(section => observer.observe(section))
      }
    }
  })
</script>