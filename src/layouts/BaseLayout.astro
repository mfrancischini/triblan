---
import "../styles/global.css"
import '@fontsource-variable/outfit/index.css'
import '@fontsource-variable/ibm-plex-sans/index.css'

import { BaseHead, Nav } from '../components/ui'
import { ClientRouter } from 'astro:transitions'
import type { ImageMetadata } from "astro"

interface Props {
  title?: string
  description?: string
  image: ImageMetadata
}

const {
  title = 'Triblan Estudio Jurídico',
  description = 'Triblan es un estudio jurídico en Olivos especializado en derecho laboral, accidentes de tránsito y defensa del consumidor.',
  image
} = Astro.props

---

<!doctype html>
<html lang="es">
  <head>
    <BaseHead title={title} description={description} image={image} />
    <ClientRouter />
  </head>
  <body class="bg-base text-ink antialiased">
    <Nav />
    <main style="padding-top: var(--nav-height);">
      <slot />
    </main>
  </body>
</html>
